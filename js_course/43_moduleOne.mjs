// создаём файлы с расширением mjs - module javascript, чтобы запустить в nodejs

const sum = (a, b) => a + b // создаём функцию и присваиваем переменной

export default sum    // экспортируем эту переменную